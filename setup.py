from setuptools import setup

setup(name='virality-headlines',
      version='0.0.1', 
      description='Virality in headlines project',
      packages=['models', 'utils', 'data'],
      zip_safe=False)
